name: Generate filtered release notes

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: "Existing tag to test release notes filter"
        required: true

permissions:
  contents: write

jobs:
  manuscript_release_notes_and_link:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout full history
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Generate filtered notes + update release + patch index.html
        uses: actions/github-script@v7
        with:
          script: |
           
            name: Generate filtered release notes

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: "Existing tag to test release notes filter"
        required: true

permissions:
  contents: write

jobs:
  manuscript_release_notes_and_link:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout full history
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Generate filtered notes + update release + patch index.html
        uses: actions/github-script@v7
        with:
          script: |
            # ← replace *everything below this line* with the new JavaScript block
